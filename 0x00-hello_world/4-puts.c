#include<stdio.h>
#include<string.h>
/**
 *main-to return to 0
 *Return: 0
 */
int main(void)
{
char code[52];
strcpy(code, "\"Programming is like building a multilingual puzzle");
puts(code);
return (0);
}
