#include<stdio.h>
/**
 *main- if sttatement
 *
 *
 *Return: return 0
 */
int main(void)
{
if (n > 0)
{
printf(" %d is positve\n", n);
}
else
{
printf(" "%d" is negative\n", n);
}
if (n == 0)
{
printf(" "%d" is zero\n", n);
 return (0);
}
